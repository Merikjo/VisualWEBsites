
@{
    ViewBag.Title = "Ratas";
}

<link rel="stylesheet" href="http://www.w3schools.com/lib/w3.css">
<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Tangerine">
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">


@section scripts {
    <script src="~/Scripts/modernizr-2.8.3.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
    <script src="/Scripts/bootstrap.js"></script>
    <script src="/Scripts/respond.js"></script>
    <script src="~/Scripts/jquery-3.1.1.js"></script>
    <script src="~/Scripts/jquery-2.2.4.js"></script>
    <script src="http://code.jquery.com/jquery.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

    @Scripts.Render("~/bundles/jqueryval")


<script type="text/javascript">
         var myGamePiece;
         var myBackground;
   
         function startGame() {
             myGamePiece = new component(500, 500, src = "~/Images/Ratasopacity.png", 500, 500, "image");
             myBackground = new component(656, 270, "Ratasopacity.png", 0, 0, "image");

             myGameArea.start();
         }

         var myGameArea = {
             canvas: document.createElement("canvas"),

             start: function () {

                 this.canvas.width = 1080;
                 this.canvas.height = 1920;
                 this.context = this.canvas.getContext("2d");


                 document.body.insertBefore(this.canvas, document.body.childNodes[0]);
                 this.frameNo = 0;
                 this.interval = setInterval(updateGameArea, 20);
             },
             stop: function () {
                 clearInterval(this.interval);
             },
             clear: function () {
                 this.context.clearRect(0, 0, this.canvas.width, this.canvas.height);
             }
         }

         function component(width, height, color, x, y, type, ctx) {
             ctx.arc(0, 0, radius, 0, 2 * Math.PI);
             this.type = type;
             if (type == "image") {
                 this.image = new Image();
                 this.image.src = Image;
             }

             this.width = width;
             this.height = height;
             this.angle = 0;
             this.x = x;
             this.y = y;
             this.update = function () {
                 ctx = myGameArea.context;
                 ctx.save();
                 ctx.translate(this.x, this.y);
                 ctx.rotate(this.angle);
                 ctx.fillStyle = Image;
                 ctx.fillRect(this.width / -2, this.height / -2, this.width, this.height);
                 ctx.restore();
             }
         }

         function updateGameArea() {
             myGameArea.clear();
             myGamePiece.angle += 1 * Math.PI / 180;
             myGamePiece.update();
         }

</script>


}

@section head {
<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
@*<link href="~/Sectionscripts/carousel.css" rel="stylesheet" />*@
<link href="~/Sectionscripts/Mysite.css" rel="stylesheet" />
<link href="~/Content/Site.css" rel="stylesheet" />
    <style>

        body {
            background-color: rgb(232,232,232);
        }
         div.background {
            background-image: url('../../images/leimaseina.jpg');
            border: 2px solid black;
            margin: 0px;
            width:100%;
            height: 1920px;
        }
        div.canvas {
            background-color: #ffffff;
            opacity: 0.5;
            filter: alpha(opacity=60); /* For IE8 and earlier */
            text-align: center;
            width: 100%;       
            height: 1920px;
        }

        div.transbox2 {
           
            background-color: #b6d8dc; /*t.harmaa*/
            border: 1px solid black;
            opacity: 0.8;
            filter: alpha(opacity=60); /* For IE8 and earlier */
            width: 100%;      
            height: 1920px;
        }

        div.transbox3 {                 
            margin-top: 50%;
        }

        div.transbox p {
      
            font-weight: bold;
            color: #393737;
            font-size: 100px;
            width: 100%;    
            height: 1920px;
        }
       
         div.col-sm-4 {
            position: absolute;
            margin-top: 50%;
            margin-bottom: 50%;
            margin-left: 50%;
            margin-right: 50%;
            /*text-align: center;*/
        }
          div.teksti {
            position: absolute; /*rivittää tekstin ruutuun*/         
            /*width: 100%; */         
            font-size: 60px;
            font-weight: bold;
            /*padding-left:5%;*/    
            text-align: justify;                  
        }
          .img-circle {          
            border-radius:50%;     
            width:800px;
            height:800px;                
}
    </style>
}


@*<body class="w3-light-grey" onload="startGame()">*@

        <!-- !PAGE CONTENT! -->
        <div class="w3-main" style="margin-left:0px;margin-top:20px;">

            <!-- Header -->
            <header class="w3-container" style="padding-top:22px">
                <h5><b><i class="fa fa-dashboard"></i> </b></h5>
            </header>

            <!--BACKGROUND-->
            <div class="background">
                <div class="canvas">
                    <div class="transbox2">
                        <div class="transbox3">
                            <div id="myDIVpyorre3">
                                <img class="img-circle" src="~/Images/Ratasopacity.png" alt="ratasopacity">@*<text fill="#ffffff" font-size="45" font-family="verdana" x="200" y="90">stamp</text>*@
                            </div>
                        </div>
                    </div>
                </div>
            </div><!--BACKGROUND END-->
        </div>

    <script src="~/Scripts/jquery-2.2.4.min.js"></script>
    <script src="~/Scripts/jquery.validate.min.js"></script>
    <script src="~/Scripts/jquery.validate.unobtrusive.js"></script>
@*</body>*@
